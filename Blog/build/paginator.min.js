/*! tgbowman.github.io 2017-11-02 */

function paginate(e){let t;t="click"===e.type?parseInt(e.target.id):e,current=t,allBlogs.forEach(function(e){!1===e.classList.contains("hidden")&&e.classList.add("hidden")});for(let e=t;e<t+blogsPerPage;e++){let t=allBlogs[e];t&&t.classList.toggle("hidden")}arrowCheck()}function arrowCheck(){prevPage.style.display=0===current?"none":"inline",nextPage.style.display=current===idCounter-blogsPerPage?"none":"inline"}function prev(){paginate(current-blogsPerPage)}function next(){paginate(current+blogsPerPage)}const blogsTotal=blogArchive.length,blogsPerPage=5,numberOfPages=Math.ceil(blogsTotal/blogsPerPage),pageButtons=document.getElementById("pageButtons");let array=document.getElementsByClassName("blog__article");const allBlogs=Array.from(array);let current=0,pageString="";pageString+="<button id ='prev'> Prev </button>";let idCounter=0;for(let e=0;e<numberOfPages;e++)pageString+=`<button id = "${idCounter}" class = "page__${e+1}">${e+1}</button>`,idCounter+=blogsPerPage;pageString+="<button id = 'next'> Next </button>",pageButtons.innerHTML=pageString;let prevPage=document.getElementById("prev"),nextPage=document.getElementById("next"),buttonCounter=0;for(let e=0;e<numberOfPages;e++)document.getElementById(buttonCounter).addEventListener("click",paginate),buttonCounter+=blogsPerPage;prevPage.addEventListener("click",prev),nextPage.addEventListener("click",next),paginate(0);